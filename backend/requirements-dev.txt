# Test / development dependencies. Installed in CI (see .github/workflows/ci.yml)
# but NOT in the production Docker image, which uses requirements.txt only.
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
